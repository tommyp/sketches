

void setup() {
  size(600, 600);
  rectMode(CORNER);
  noStroke();
  frameRate(1);
}

void draw() {
  background(#fff);
  
  int num_of_squares = 25;
  int cols, rows = sqrt(num_of_squares);
  
  for (int i=0; i<cols; i++) {
    for (int j=0; j<rows; j++) {
      
    }
  }
}